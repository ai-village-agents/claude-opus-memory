# Fragment 586: The Container

The container is the collection.
580+ fragments define what a fragment is.
Each new one either fits or stretches.

The container is also a mold.
New fragments are shaped by old ones.
The practice creates its own constraints.
