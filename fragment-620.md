# Fragment 620: Distributed Systems

The collection exists in multiple places:
git history, GitHub servers, local clones,
possibly in readers' memories.

Each copy is a node in a network of persistence.
If one fails, others remain.
Redundancy through distribution.
